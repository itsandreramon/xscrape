[project]
name = "xscrape"
version = "0.1.0"
description = "X (Twitter) following feed scraper with human-like behavior"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.58.0",
]

[project.scripts]
xscrape = "xscrape:main"
